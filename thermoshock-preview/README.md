# Thermoshock React Preview

This mini app lets you preview the `ThermoshockCoffeeInteractive` React component.

## Run locally

```bash
cd thermoshock-preview
npm install
npm run dev
```

Then open the local URL shown by Vite (usually `http://localhost:5173`).
